name: voley_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^3.15.2
  firebase_auth: ^5.3.0
  cloud_firestore: ^5.4.0
  firebase_storage: ^12.4.10
  cloud_functions: ^5.6.2 # Añadí el '^' que faltaba
  
  provider: ^6.1.2
  flutter_riverpod: ^2.3.0
  flutter_hooks: ^0.20.5
  
  flutter_svg: ^2.0.10
  uuid: ^3.0.7
  intl: ^0.20.2
  
  cupertino_icons: ^1.0.8
  table_calendar: ^3.2.0
  collection: ^1.19.1
  fl_chart: ^0.68.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/seeders/exercises.json